import React from "react";
import {
  colorPrimary
} from "./variable";

const AppBar = () => (
  <div
    style={{
      height: "45px",
      width: "100%",
      backgroundColor: colorPrimary,
    }}
  />
);

export default AppBar;
